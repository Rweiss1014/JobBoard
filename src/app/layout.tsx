import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "L&D Exchange | Learning & Development Jobs & Freelancers",
  description:
    "The premier job board and freelancer directory for Learning & Development professionals. Find instructional design, e-learning, and corporate training opportunities.",
  keywords: [
    "instructional design jobs",
    "e-learning developer jobs",
    "L&D jobs",
    "learning and development careers",
    "corporate training jobs",
    "instructional designer freelance",
    "e-learning freelancer",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-gray-50`}
      >
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
